# XianYu (闲鱼) adapter for E-SP-Line2
# Python runtime dependencies

# Core HTTP / WS
requests>=2.31.0
websockets>=12.0

# Logging
loguru>=0.7.2

# Data models (vendor/message/types.py)
pydantic>=2.0.0

# Sign JS execution (vendor/static/*.js)
# 需同时安装 Node.js 18+ 运行时
# 注意: 安装包名为 PyExecJS（import 名为 execjs）
PyExecJS>=1.5.1

# Protobuf 消息解密 (vendor/utils/goofish_utils.py)
# 注意: PyPI 上 blackboxprotobuf 最新为 1.0.1，勿写 >=3.0.0
blackboxprotobuf>=1.0.1

# Optional: QR code login
qrcode>=7.4.2

# Optional: retry
backoff>=2.2.1
