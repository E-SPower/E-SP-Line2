# Optional: backend origin override.
# Default (empty) uses the Vite dev proxy /api + /ws, or a reverse proxy in production.
# Set this only when the API is served from a different origin than the frontend.
# VITE_API_BASE_URL=http://localhost:8080
